import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  card: {
    marginTop: 12,
    marginHorizontal: 12,
  },
  content: {
    padding: 10,
  },
});

export default styles;
