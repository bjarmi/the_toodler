/*
 * This module contains the logger middleware
 * https://www.npmjs.com/package/redux-logger
 * */

import { createLogger } from "redux-logger";

const Logger = createLogger();
export default Logger;
